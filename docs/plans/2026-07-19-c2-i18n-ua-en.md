---
status: planned
created: 2026-07-19
updated: 2026-07-19
related: "[[ROADMAP]]"
tags: [plan, i18n, c2]
---

# План C2: локалізація UA/EN (для виконавця на молодшій моделі)

Давно відкладений план (відкладався з Етапу C). Мета: весь видимий UI
двомовний — українська за замовчуванням, англійська через перемикач.
Бета буде з українськими астрологами, тож укр-локаль — не прикраса,
а умова беты.

**Порядок відносно решти ROADMAP:** цей план виконується ПЕРЕД повним
security-рев'ю. Причина: локалізація торкнеться майже кожного
фронтенд-файлу, і рев'ю коду, який от-от буде перелопачений, знецінюється.
Рев'ю — наступний етап після C2, по фінальному коду.

## Ключові архітектурні рішення (прийняті, не переглядати без згоди власника)

1. **БЕЗ URL-роутингу локалей.** Жодних `app/[locale]/…`, жодного переносу
   файлів. next-intl працює в режимі "without i18n routing": локаль
   визначається cookie `NEXT_LOCALE`, дефолт — `uk`. Причини: (а) наш
   `proxy.ts` — власний auth-middleware зі списками PUBLIC_PATHS по
   `startsWith` — композиція з i18n-middleware то окреме мінне поле;
   (б) перенос 39 файлів у `[locale]` — величезний churn без потреби.
   Ціна: немає окремих URL для мов (SEO/hreflang) — свідомо відкладено
   пост-launch, зафіксовано в «Не робити».
2. **Дефолт — українська.** Ринок український, `html lang="uk"` вже стоїть,
   легальні сторінки українською. Новий відвідувач без cookie бачить укр.
3. **Легальні сторінки (/privacy, /terms) НЕ перекладаються** — лишаються
   українською в обох локалях. Юридичний текст в одному екземплярі, без
   ризику розбіжностей перекладу. (На EN-локалі вони просто українські.)
4. **Бекенд не чіпаємо**, окрім двомовних email-шаблонів (Частина 5).
   Фічі тарифів приходять з бекенда англійською (PLANS у billing.py) —
   фронтенд перекладає їх словником «рядок → переклад» з fallback на
   оригінал. API-помилки (detail-рядки) лишаються англійською.

## Ключові факти середовища (перевірено 2026-07-19)

> - **next-intl 4.13.2 офіційно підтримує Next ^16.0.0** (перевірено
>   `npm info next-intl peerDependencies`). Але ЧИТАЙ доки next-intl для
>   App Router "without i18n routing" перед налаштуванням — і пам'ятай
>   AGENTS.md: Next 16 може відрізнятись від training data, доки в
>   `node_modules/next/dist/docs/`.
> - Фронтенд: 39 tsx-файлів у `app/`. Основні поверхні:
>   (auth) — login, register, forgot-password, reset-password (+форма),
>   verify-email (+action), layout; (dashboard) — layout (NAV, футер,
>   хедер, мобільний NAV), dashboard, persons (+PersonForm, PersonList),
>   charts (+ChartForm, TransitForm, SolarReturnForm, SynastryForm,
>   SavedChartsTab, ChartTabs, ResultTables, SaveChartButton), account
>   (+_EmailSection, _DangerZone), billing (+ManageButton, RenewButton),
>   pricing (+PricingCard); спільні — _EmailVerificationBanner,
>   UpgradePrompt, CityAutocomplete, ChartWheel; корінь — layout.tsx
>   (metadata укр), page.tsx (лендинг, англ).
> - `next.config.ts` вже має `headers()` (5 security-заголовків з Етапу D) —
>   плагін next-intl обгортає конфіг; ПІСЛЯ підключення перевірити
>   `curl -sI`, що заголовки не зникли.
> - `proxy.ts` при токен-рефреші ВРУЧНУ зшиває Cookie-хедер
>   (рядки ~81-82: `existingCookie; access_token=...`) — перевірити, що
>   `NEXT_LOCALE` переживає рефреш токена (залогінитись, дочекатись/
>   форсувати рефреш, локаль не злетіла).
> - Дати зараз: `toLocaleDateString("en-GB")` у PersonList (хардкод!),
>   `toLocaleDateString()`/`toLocaleString(undefined, …)` у SavedChartsTab,
>   SolarReturnForm, billing/page — усі мають стати локаль-залежними.
> - `lib/house-systems.ts` — labels систем будинків (Placidus, Koch…);
>   назви систем — власні імена, можна лишити, але підписи форм — ні.
> - `ChartWheel` рендерить астрологічні ГЛІФИ (♈︎ ☉ …) — не чіпати; текстові
>   назви планет/знаків у таблицях (ResultTables та ін.) — перекладати
>   через astro-словник.
> - Бекенд-тести: 107 passed / 2 skipped. `tests/test_email_flow.py` мокає
>   send_email — перевірити, чи асерти зав'язані на текст листів, перш ніж
>   міняти шаблони (Частина 5).
> - pytest/tsc/build інваріанти ті самі, що в попередніх планах; коміт після
>   кожної частини + пуш; в кінці синк main + `gh run watch` (3 job).

## Прогрес

- [x] Частина 1 — Інфраструктура next-intl (cookie-режим, дефолт uk) + перемикач мови:
      `next-intl@4.13.2` встановлено; `i18n/request.ts` (LOCALES=[uk,en],
      DEFAULT_LOCALE=uk, читає cookie `NEXT_LOCALE`); `next.config.ts`
      обгорнуто `createNextIntlPlugin()`; `messages/{uk,en}.json` — скелет
      усіх 12 неймспейсів; `app/layout.tsx` — `NextIntlClientProvider` +
      `html lang={await getLocale()}` (був хардкод "uk"). Server action
      `setLocale` (app/actions/locale.ts) + клієнтський `LanguageSwitcher`
      (app/_components/LanguageSwitcher.tsx, useLocale+useTranslations)
      розміщено в 4 місцях: сайдбар дашборда, мобільний хедер дашборда,
      (auth)-layout (кут), лендинг (кут). Живо перевірено: перемикач
      uk→en міняє html lang і весь рендер (включно з server components);
      cookie `NEXT_LOCALE` видна в document.cookie (не httpOnly, як і
      планувалось); переживає повне перезавантаження сторінки;
      `curl -sI` — усі 5 security-заголовків на місці після плагіна.
      ПОЗАПЛАНОВА знахідка при перевірці 1.3: план припускав, що досить
      скоротити `ACCESS_TOKEN_EXPIRE_MINUTES` на бекенді, але
      proxy.ts-рефреш тригериться ВІДСУТНІСТЮ cookie `access_token`
      (`request.cookies.has(...)`), а не валідністю JWT всередині —
      простий бекенд-тест дав 401 на `/auth/me` без жодного виклику
      `/auth/refresh`. Виправив методику: тимчасово скоротив maxAge
      самої cookie в `lib/auth.ts` (15с), зафіксував живий виклик
      `POST /auth/refresh → 200`, підтвердив `NEXT_LOCALE` і `html lang`
      пережили цикл рефреша, відкотив зміну (`git diff` — чисто).
      Побічний ефект build: кілька раніше статичних роутів (/login,
      /register, /privacy, /terms, /_not-found) стали динамічними (ƒ) —
      неминучий наслідок читання `cookies()` у кореневому layout для
      визначення локалі; узгоджується з рішенням №1 плану (cookie-режим
      без URL-роутингу), не баг.
- [x] Частина 2 — Словники: auth-сторінки + chrome дашборда (NAV, банери, футер):
      неймспейси `nav`, `auth.{login,register,forgotPassword,resetPassword,
      verifyEmail,verificationBanner,errors}`, `dashboard`,
      `common.upgradePrompt` заповнено обома мовами. Оновлено: login,
      register, forgot-password, reset-password (+форма), verify-email
      (+action), _EmailVerificationBanner, UpgradePrompt (додано "use client"
      — тепер використовує useTranslations), (dashboard)/layout.tsx (NAV
      тепер масив, побудований з `getTranslations("nav")`, а не константа),
      dashboard/page.tsx (рядки "Getting started" через `t.rich()` з
      вкладеними `<Link>` — ICU rich text, не звичайна інтерполяція).
      ПОЗАПЛАНОВА, але суттєва знахідка: `app/actions/auth.ts` (7 server
      actions) виявився ПОВНИЙ хардкоджених фронтенд-рядків ("Cannot connect
      to server", "Invalid email or password" і т.д.) — це НЕ backend
      detail-рядки, як можна було подумати з побіжного погляду на
      сторінки; лише ОДИН рядок (registrationFailed fallback) справді
      зав'язаний на `body.detail` з бекенда. Усі статичні рядки
      перекладено через `getTranslations` (next-intl/server) напряму
      всередині server actions — підтверджено живцем, що це працює
      (не лише в Server Components): форма логіну з невірним паролем
      → "Невірний email або пароль" на uk, "Invalid email or password"
      після перемикання на en і ПОВТОРНОГО сабміту (стара помилка
      лишається тою мовою, якою була на момент сабміту — це очікувано,
      не баг). curl -sI — заголовки на місці; tsc/build чисті;
      pytest 107/2 (бекенд не чіпався).
- [x] Частина 3 — Charts + persons: форми, таблиці, astro-словник (знаки/планети/аспекти):
      `app/lib/astro-i18n.ts` (NEW) — `useAstroTranslator()` через
      `useMessages()` (сирий об'єкт повідомлень), НЕ через типізований
      `t()`: у next-intl немає `t.has()`-перевірки існування ключа, а
      назви планет/знаків/аспектів приходять з бекенда як рантайм-рядки
      (не compile-time відомі ключі) — типізований `t()` з цим погано
      дружить. `(category, raw) => dict[raw.toLowerCase()] ?? capitalize(raw)`.
      Заповнено `astro.{planets×13,signs×12,aspects×11,retrograde,applying,
      separating,house}` в обох locale-файлах. Оновлено: ResultTables
      (PlanetTable/AspectTable/ArabicPartsTable — заголовки + назви через
      astro-словник; Arabic part names СВІДОМО НЕ перекладено, коментар
      у коді), ChartForm/TransitForm/SolarReturnForm/SynastryForm
      (лейбли, кнопки, заголовки панелей), ChartTabs, SaveChartButton,
      SavedChartsTab (CHART_TYPE_LABELS тепер component-local, не
      module-level const — бо залежить від `t`), PersonForm (`t.rich()`
      для «Saved: **name**»), PersonList (кнопки, empty state, підтвердження
      видалення), persons/page.tsx, charts/page.tsx. Дати: виправлено
      хардкод `en-GB` у PersonList (без урахування locale — баг з факту
      плану), SolarReturnForm, SavedChartsTab на
      `locale === "uk" ? "uk-UA" : "en-GB"`.
      ChartWheel.tsx: гліфи/градуси/ASC-DSC-MC-IC НЕ чіпав (як і
      планувалось) — переклав тільки текст hover-tooltip-ів у двох
      під-компонентах (`AspectLines`, `PlanetLayer`), які раніше містили
      суміш захардкодженого англ ("Applying"/"Separating", "House {n}",
      "Orb …") і навіть десь укр — тепер обидва через `useAstroTranslator()`
      + `useTranslations("astro"|"charts.table")`; видалив непотрібний
      локальний `_cap()`-хелпер (став мертвим кодом після заміни всіх
      використань). Перевірено живцем hover-tooltip планети через
      dispatchEvent (реальні кліки/скрол у Browser pane були нестабільні
      в цій сесії — окрема, не пов'язана з кодом проблема інструменту
      автоматизації; get_page_text/read_page/preview_logs залишались
      надійними і підтвердили коректний рендер) — Sun-тултип показав
      «Сонце / Cap 10°41' / Дім 10», ретроградний Mercury — «℞ Ретроградний»
      і «Дім 11»: інтерполяція `ta("house", {n})` і translated-retrograde
      підтверджені. Натальний розрахунок (uk і en) живцем: заголовки
      таблиць, назви планет/знаків/аспектів, Arabic parts (лишились
      англійськими, тільки Sign-колонка перекладена) — усе коректно.
      `tsc --noEmit` чисто; `npm run build` чисто (усі роути ті самі
      `ƒ`/`○`, що й раніше); pytest 107/2 (бекенд не чіпався).
- [x] Частина 4 — Billing/pricing/account/landing + локалізовані metadata і дати:
      `app/layout.tsx` — `export const metadata` замінено на
      `generateMetadata()` через `getTranslations("common.meta")`
      (title/description/OG) — раніше опис був хардкодом укр незалежно
      від локалі. `app/lib/billing-i18n.ts` (NEW) — та сама схема, що і
      astro-словник у Частині 3 (`useMessages()`/`getMessages()` замість
      типізованого `t()`, бо ключі словника — точні англ рядки з бекенда
      `PLANS[].features`, не compile-time відомі next-intl ключі):
      `useFeatureTranslator()` (клієнт, PricingCard) і
      `getFeatureTranslator()` (сервер, billing/page, landing page).
      Заповнено `billing.features` (16 рядків з `services/astro-api/app/
      routers/billing.py::PLANS`, включно з непублічним `expert`-тарифом
      про всяк випадок) — фолбек на оригінал, якщо рядок не знайдено.
      Назви самих тарифів (Free/Pro/Expert) НЕ перекладено — свідомо,
      той самий підхід, що й до назв систем будинків у Частині 3
      (усталені назви тарифів, як у більшості SaaS). Оновлено: billing/
      page.tsx (+ generateMetadata, + виправлено хардкод-локаль дати
      `periodEndDate.toLocaleDateString()` без аргументів → `locale ===
      "uk" ? "uk-UA" : "en-GB"`, факт з плану), ManageButton, RenewButton
      (разом з текстом `alert()`), pricing/page.tsx (+ generateMetadata),
      PricingCard, account/page.tsx (+ generateMetadata), _EmailSection,
      _DangerZone, app/page.tsx (лендинг — hero, features-картки,
      pricing-teaser з тим самим feature-словником, footer). ПОЗАПЛАНОВА
      дрібниця: `charts/page.tsx` лишився з хардкодженим `title: "Charts
      — Zorya"` після Частини 3 (пропустив тоді) — виправлено тут же на
      `generateMetadata()` з `t("pageTitle")`. `curl -sI` — 5 заголовків
      на місці; `tsc --noEmit` і `npm run build` чисті (ті самі роути);
      живцем uk/en: landing (hero/features/pricing-teaser/footer),
      pricing (картки з фічами, disclaimer), billing (поточний тариф,
      usage з інтерполяцією ліміту), account (email-статус, danger
      zone) — усе коректно, включно з `<title>`-тегом вкладки браузера
      per-сторінково. pytest 107/2 (бекенд не чіпався).
- [x] Частина 5 — Двомовні email-шаблони (бекенд, статичні UA+EN):
      `services/astro-api/app/routers/auth.py` — додано
      `VERIFY_EMAIL_SUBJECT`/`RESET_PASSWORD_SUBJECT` (двомовний subject
      через " / ") і `_verify_email_html(link)`/`_reset_password_html(link)`
      (укр блок, `<hr>`, англ блок — обидва з тим самим лінком, TTL
      прописано в обох мовах: 24 год / 24 hours, 1 год / 1 hour). Замінено
      3 старих inline-виклики `send_email(..., "Confirm your Zorya
      account", f"<p>...</p>")` (register, send_verification — той самий
      шаблон verify, буквально дублювався двічі в коді) і 1 виклик reset
      (forgot_password) на виклики хелперів. Перед зміною перевірив
      `tests/test_email_flow.py`: асерти зав'язані лише на кількість
      надісланих листів і на regex `token=([A-Za-z0-9_\-]+)` для
      витягання токена з HTML — на subject/текст листа НЕ зав'язані,
      тож зміна безпечна. pytest повний прогін: 107 passed / 2 skipped
      (усі 7 тестів test_email_flow.py — зелені без жодної правки самих
      тестів). Live-перевірка: викликав `_verify_email_html`/
      `_reset_password_html` напряму з реальним token-лінком — обидва
      блоки (укр+англ) рендеряться коректно, посилання робоче в обох.
- [ ] Частина 6 — Наскрізна верифікація обома мовами + доки + синк main
- [ ] DoD — див. внизу

---

## Частина 1 — Інфраструктура

### 1.1. Установка й конфіг
- `npm i next-intl` (4.13.x).
- Прочитати в доках next-intl розділ **"App Router → without i18n routing"**
  (доки в node_modules або на сайті) — сетап відрізняється від роутованого!
- `i18n/request.ts`: `getRequestConfig` — локаль з cookie `NEXT_LOCALE`
  (через `cookies()` з `next/headers`), валідні значення `uk | en`,
  усе інше/відсутнє → `uk`.
- `next.config.ts`: обгорнути `createNextIntlPlugin()` — існуючий
  `headers()` зберегти. Після: `curl -sI http://localhost:3000/login` —
  всі 5 заголовків на місці.
- `messages/uk.json`, `messages/en.json` — скелет неймспейсів:
  `common`, `nav`, `auth`, `dashboard`, `persons`, `charts`, `astro`,
  `billing`, `pricing`, `account`, `landing`, `errors`.
- `app/layout.tsx`: `NextIntlClientProvider` + `html lang={locale}`
  (зараз hardcoded "uk"); metadata поки не чіпати (Частина 4).

### 1.2. Перемикач мови
- Server action `setLocale(locale)`: ставить cookie `NEXT_LOCALE`
  (httpOnly НЕ потрібен — це не секрет; maxAge ~рік, path=/) і робить
  `revalidatePath("/", "layout")` або еквівалент, щоб серверні компоненти
  перерендерились.
- Клієнтський `LanguageSwitcher` (компактний «UK | EN», активна мова
  виділена): розмістити (а) у футері сайдбара дашборда поруч із
  Privacy/Terms, (б) у мобільному хедері або мобільному NAV-рядку,
  (в) на (auth)-layout і лендингу (кут сторінки).
- Верифікація: перемкнути → УСЯ сторінка (включно з server components)
  перерендерилась іншою мовою; cookie видно в devtools; після
  повного перезавантаження мова збереглась.

### 1.3. Перевірка виживання куки при токен-рефреші
Залогінитись, дочекатись протухання access_token (30 хв — АБО тимчасово
виставити менший `ACCESS_TOKEN_EXPIRE_MINUTES` в .env бекенда для тесту),
зробити навігацію, що тригерить рефреш у proxy.ts → локаль НЕ скинулась.
Якщо скинулась — фікс у proxy.ts (зшивання Cookie-хедера губить куки).

---

## Частина 2 — Auth + chrome

- Екстракція рядків у словники (обидві мови одразу, укр — основний текст,
  не машинний підрядник):
  - login, register, forgot-password, reset-password, verify-email
    (заголовки, лейбли, плейсхолдери, кнопки, лінки «No account? Create
    one», повідомлення станів — успіх/помилка).
  - `(dashboard)/layout.tsx`: NAV (Overview/Persons/Charts/Reports/
    Pricing/Billing/Account), Sign out, Privacy/Terms/Feedback-лейбли.
  - `_EmailVerificationBanner`, `UpgradePrompt` («Pro plan required…»),
    dashboard/page (Overview, Getting started…).
- Патерни next-intl: server components — `getTranslations`, client —
  `useTranslations`; перевір у доках точні імена для v4.
- Помилки з server actions (auth.ts повертає рядки з бекенда) — НЕ
  перекладати динамічні detail-рядки; статичні фронтенд-повідомлення
  («Cannot connect to server» тощо) — у словник.

## Частина 3 — Charts + persons

- `astro`-словник: 12 знаків зодіаку, планети (Sun, Moon, Mercury, Venus,
  Mars, Jupiter, Saturn, Uranus, Neptune, Pluto, True_node → «Півн. вузол»,
  Lilith → «Ліліт», Chiron), назви аспектів (conjunction → «з'єднання» …)
  для title-атрибутів таблиць. Ключі — АНГЛІЙСЬКІ канонічні рядки, як їх
  віддає бекенд (lowercase) → хелпер `translateAstro(name, t)` з fallback
  на капіталізований оригінал.
- ResultTables (заголовки колонок + назви планет/частин), ChartForm,
  TransitForm, SolarReturnForm, SynastryForm (лейбли, кнопки
  Calculate…, заголовки панелей), ChartTabs (Natal/Transit/…/Saved),
  SaveChartButton, SavedChartsTab (Title/Type/Saved/Actions, empty state).
- Arabic parts: назв ДУЖЕ багато (100+ рядків з бекенда) — НЕ перекладати
  в цьому плані (фіксуємо в «Не робити»), лишаються англійською в обох
  локалях. Заголовок таблиці — перекладати.
- Persons: PersonForm (лейбли), PersonList (empty state, кнопки), дати —
  `toLocaleDateString(locale === "uk" ? "uk-UA" : "en-GB", …)` замість
  хардкоду.
- ChartWheel: гліфи/градуси не чіпати; якщо є текстові tooltips — у словник.
- Плюрали укр (one/few/many) через ICU: `{count, plural, …}` — там, де є
  лічильники (usage «charts calculated today» тощо).

## Частина 4 — Billing / pricing / account / landing / metadata

- billing/page: банери (успіх/«не отримали платіж»), CURRENT PLAN,
  «Active until … — no auto-renewal» (дата локаллю!), usage-блок,
  ManageButton/RenewButton.
- pricing: заголовки, футер-дисклеймер (укр версія — та сама чесність про
  «30 днів без автопродовження»), кнопки оплати; фічі тарифів з бекенда —
  словник «канонічний англ рядок → переклад» + fallback (бекенд НЕ чіпати).
- account: Email-секція, Danger zone (текст незворотності — точний переклад).
- landing (app/page.tsx): увесь текст + CTA.
- `app/layout.tsx`: `generateMetadata` за локаллю (title/description/OG) —
  зараз description укр хардкодом; en-версію додати.
- robots/sitemap: НЕ чіпати (роутинг один).

## Частина 5 — Двомовні email-шаблони (бекенд)

- `routers/auth.py`: verify- і reset-листи зробити статично двомовними в
  одному листі (укр блок, нижче англ), бо локаль юзера бекенд не знає і
  зберігати її — поза скоупом. Subject: `"Підтвердіть акаунт Zorya / Confirm
  your Zorya account"` (аналогічно для reset).
- Перевірити `tests/test_email_flow.py`: якщо асерти на текст листа —
  оновити; якщо тільки на факт виклику/токен — не чіпати.
- pytest повний прогін.

## Частина 6 — Наскрізна верифікація + доки

1. Браузером ОБОМА мовами: landing → register → dashboard → persons →
   charts (natal розрахунок, таби) → account → pricing → billing →
   sign out → login. Скріншоти ключових сторінок в обох локалях.
2. Чистий контекст (нова сесія браузера без cookie) → перший рендер
   українською.
3. Перемикач: uk→en→uk, перезавантаження, перехід між сторінками —
   мова стабільна.
4. `curl -sI` — security-заголовки живі; `tsc --noEmit && npm run build`;
   pytest.
5. Хардкод-аудит: `grep` по компонентах на залишки характерних англ
   рядків (Sign in, Calculate, Upgrade…) поза словниками — на укр локалі
   на ключових сторінках не повинно бути видимого англ UI (крім
   свідомих винятків: Arabic parts, назви систем будинків, легальні
   сторінки, API-помилки).
6. Доки: ROADMAP (відмітити C2), plans README, цей файл (Прогрес + DoD).
   Коміти по частинах + пуш; синк main + `gh run watch` — 3 job зелені.

---

## Ризики
1. next-intl-плагін у next.config.ts може загубити `headers()` — перевірити
   curl-ом одразу в Частині 1, не в кінці.
2. `proxy.ts` вручну зшиває Cookie-хедер при рефреші — NEXT_LOCALE може
   загубитись саме там; тест у 1.3 обов'язковий.
3. Патерни v4 next-intl (getTranslations/useTranslations/setRequestLocale)
   можуть відрізнятись від training data — читати доки пакета, не пам'ять.
4. Укр плюрали (one/few/many/other) — не забути `few` (2-4 карти).
5. Не перекласти повідомлення-стан (помилки форм) — легко пропустити;
   хардкод-аудит у Частині 6 їх ловить.
6. Подвійна поверхня друкарських помилок — фінальна вичитка укр текстів
   одним проходом.
7. Великий diff по 30+ файлах — комітити по частинах, не одним мегакомітом.

## Не робити (поза скоупом)
- URL-роутинг локалей (`/en/...`), hreflang, локалізований sitemap —
  окрема пост-launch SEO-задача.
- Переклад privacy/terms (лишаються українською в обох локалях).
- Локалізація API-помилок бекенда і збереження локалі юзера в БД.
- Переклад назв Arabic parts (100+ рядків з бекенда) і назв міст атласу.
- Треті мови, RTL.

## Definition of Done
1. Обидві локалі повністю покривають: auth-сторінки, дашборд-chrome,
   dashboard, persons, charts (усі 5 табів + таблиці + astro-назви),
   account, billing, pricing, landing.
2. Новий відвідувач без cookie бачить українську; перемикач працює з
   усіх поверхонь і персистить через cookie (включно з токен-рефрешем).
3. Дати всюди рендеряться локаллю користувача (жодного хардкоду "en-GB").
4. Email-листи двомовні; pytest 0 failed.
5. `tsc --noEmit && npm run build` чисті; security-заголовки на місці.
6. Хардкод-аудит пройдено (свідомі винятки задокументовані в цьому файлі).
7. Коміти по частинах + пуш, синк main, CI зелений (3 job).
